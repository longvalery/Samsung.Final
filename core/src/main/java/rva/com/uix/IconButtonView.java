package rva.com.uix;

public class IconButtonView extends ButtonView {
    public IconButtonView(float x, float y, float size, String texturePath) {
        super(x, y, size, size, texturePath);
    }
}
